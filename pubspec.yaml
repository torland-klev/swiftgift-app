name: gaveliste_app
description: 'Gavelista App'

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  firebase_core: ^3.2.0
  google_sign_in: ^6.2.1
  flutter_dotenv: ^5.1.0
  http: ^1.2.1
  sign_in_button: ^3.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/gift-background.png
